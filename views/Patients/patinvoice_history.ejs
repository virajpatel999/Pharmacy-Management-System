<%- include('../partials/header'); -%>
<title>Invoice History | VIT Pharmacy</title>

</head>

<body>
<%- include('../partials/navbar'); -%>

<div class="container-fluid">
  <div class="row">
    <%- include('../partials/patsidebar'); -%>
    <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-md-4">
        <div class="container-fluid">
          <br>
          <!-- <div class="table-responsive">
            <h2>Medicine History</h2>
            <table class="table table-striped table-bordered table-hover" id="myTable">
              <thead class="thead-dark">
                <tr>
                  <th>Medicine Name</th>
                  <th>Date of purchase</th>
                  <th>Quantity Purchased</th>
                </tr>
              </thead>
              <tbody>
                <% rows.forEach(function(bill){ %>
                  <tr>
                    <td><%= bill.med_name %></td>
                    <td><%= bill.bill_date %></td>
                    <td><%= bill.quantity %></td>
                  </tr>
                <% }); %>
              </tbody>
            </table> -->
          </div>
            <h2>Invoice History</h2>
            <div class="input-group">
              <div class="input-group-prepend">
                <!-- <div class="input-group-text"><i class="fas fa-search"></i></div> -->
              </div>
              <!-- <input class="form-control" id="searchBill" type="text" onkeyup="searchBill()" placeholder="Search a bill by patient name"> -->
            </div>
            <br>
            <!-- <div class="table-responsive">
              <table class="table table-striped table-bordered table-hover" id="myTable">
                <thead class="thead-dark">
                  <tr>
                    <th></th>
                    <th>Bill No.</th>
                    <th>Payment Mode</th>
                    <th>Bill Amount</th>
                    <th>Discount</th>
                    <th>Billing Date</th>
                  </tr>
                </thead>
                <tbody>
                  <% rows.forEach(function(bill){ %>
                    <ul class="pagination">
                    <tr>
                      <td><a style="color: black; text-decoration: none;" href="/pat_billNo<%= bill.bill_no %>"><i class="fas fa-external-link-alt"></i></a></td>
                      <td><%= bill.bill_no %></td>
                      <td><%= bill.payment_mode %></td>
                      <td><%= bill.total_cost %></td>
                      <td><%= bill.discount %></td>
                      <td><%= bill.bill_date %></td>
                    </tr>
                    </ul>

                  <% }); %>
                </tbody>
              </table>
            </div> -->
            <!-- <div class="table-responsive">
              <table class="table table-striped table-bordered" id="billTable" style="empty-cells: hide;">
                <thead class="thead-dark">
                  <tr>
                    <th>Sr. No.</th>
                    <th>Medicine Name</th>
                    <th>MRP (In &#x20B9;)</th>
                    <th>Quantity</th>
                    <th>Total</th> 
                  </tr>
                </thead>
                <tbody>
                    <% var i=0; %>
                    <% rows.forEach(function(bill){ %>
                        <tr>
                            <td><%= i+1 %></td>
                            <td>
                                <input id='medName<%= i %>' type='text' class='form-control-plaintext' value='<%= bill.med_name %>' readonly>
                            </td>
                            <td>
                                <input id='medQuantity<%= i %>' type='number' class='form-control-plaintext' value="<%= bill.quantity %>" readonly>
                            </td>
                            <td>
                                <input id='medTotal<%= i %>' type='number' class='form-control-plaintext' readonly>
                            </td>
                        </tr>
                        <% ++i; %>    
                    <% }); %>
                </tbody> -->

                <div class="table-responsive">
                  <table class="table table-striped table-bordered table-hover" id="myTable">
                    <thead class="thead-dark">
                      <tr>
                        <th></th>
                        <th>Bill No.</th>
                        <th>Payment Mode</th>
                        <th>Bill Amount</th>
                        <th>Discount</th>
                        <th>Billing Date</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% rows.forEach(function(bill){ %>
                        <tr>
                          <td><a style="color: black; text-decoration: none;" href="/pat_billNo<%= bill.bill_no %>"><i class="fas fa-external-link-alt"></i></a></td>
                          <td><%= bill.bill_no %></td>
                          <td><%= bill.payment_mode %></td>
                          <td><%= bill.total_cost %></td>
                          <td><%= bill.discount %></td>
                          <td><%= bill.bill_date %></td>
                        </tr>
                      <% }); %>
                    </tbody>
                  </table>
                </div>
        </div>
    </main>
    </div>
</div>
<script>
    function searchBill() {
        var input, filter, table, tr, td, i, txtValue;
        input = document.getElementById("searchBill");
        filter = input.value.toUpperCase();
        table = document.getElementById("myTable");
        tr = table.getElementsByTagName("tr");
        for (i = 0; i < tr.length; i++) {
            td = tr[i].getElementsByTagName("td")[1];
            if (td) {
            txtValue = td.textContent || td.innerText;
            if (txtValue.toUpperCase().indexOf(filter) > -1) {
                tr[i].style.display = "";
            } else {
                tr[i].style.display = "none";
            }
            }       
        }
    }
</script>
</div>
</main>
</div>
</div>
</body>
<%- include('../partials/footer'); -%>